#include <bits/stdc++.h>

using namespace std;

const int Nmax = 3 * 1e5 + 3;
vector<vector<int>> rank;

int main(){
    int n, w, points[Nmax];
    scanf("%d %d", &n, &w);
    for(int i = 1; i <= w; i++){
        int k, m = 1;
        scanf("%d", &k);
            while(k--){
                int temp;
                scanf("%d", points[temp]);
            }
            sort(points + 1, points + n + 1);
            for(int i = 1; i )
    }
    return 0;
}